// Navigation bar hidden per user request as focus shifts exclusively to Spices

const ExportNavigator = () => {
  // Navigation bar hidden per user request as focus shifts exclusively to Spices
  return null;
};

export default ExportNavigator;
